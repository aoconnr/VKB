%h1 Vintage Kitty Boutique
%br 
/ change to padding
.col-lg-2
  .sidebar
    %ul.nav.well
      //TODO get this styling out of here
      %div{:style => 'margin-left: 10px;'}
        - @categories.each do |category|
          %li.h4.a{:href => '#'}
            = category.name
            - @types.each do |type|
              - if category.id == type.category_id
                %li
                  = type.category.name

        
- @items.each do |item|
  .col-lg-3
    = link_to image_tag("/uploads/#{item.id}/thumb_#{File.basename(item.image.url)}"), item
    .caption
      %h5 
        = item.name
      = currency_symbol
      = item.cost